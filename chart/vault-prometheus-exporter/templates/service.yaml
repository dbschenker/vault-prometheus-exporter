{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "vault-prometheus-exporter.fullname" . }}
  labels:
    {{- include "vault-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vault-prometheus-exporter.selectorLabels" . | nindent 4 }}
{{- end }}
